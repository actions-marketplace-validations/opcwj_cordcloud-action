#!/usr/bin/env python3
"""
CordCloud 设备验证流程分析指南

当遇到 "检测到陌生设备，需要进行二步验证" 错误时，
按以下步骤获取设备验证的完整信息。
"""

print("""
╔════════════════════════════════════════════════════════════════╗
║         CordCloud 设备验证流程 - 信息收集指南                 ║
╚════════════════════════════════════════════════════════════════╝

【第一步】准备浏览器工具
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
1. 打开 CordCloud 官网 (https://www.cordc.net/auth/login)
2. 按 F12 打开开发者工具
3. 切换到 "Network" 标签
4. 清空网络记录（点击垃圾桶图标）

【第二步】尝试登录
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
5. 在登录表单中输入邮箱和密码
6. 点击登录按钮
7. 观察网络请求

【第三步】记录关键信息
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
当看到 "检测到陌生设备..." 错误时，按以下顺序检查：

▼ 查看登录响应（POST /auth/login）
  └─ 在 Network 标签中找到最后的 POST 请求
  └─ 点击它，查看 "Response" 标签
  └─ 记录完整的 JSON 响应，特别是：
     • ret 值 (1 = 成功, 0 = 失败, 3 = 需要验证？)
     • msg 内容
     • 是否有验证相关的字段 (token, verify_url, device_id, etc)

▼ 查看页面响应（GET /auth/login）
  └─ 看看返回的 HTML 中是否有验证链接或验证界面
  └─ 复制任何验证相关的 URL 或 JavaScript 代码

▼ 查看所有包含以下关键词的请求：
  • device      (例如: device-verify, verify-device, device_id)
  • verify      (例如: verify, verification, verify-confirm)
  • token       (令牌验证)
  • confirm     (确认操作)
  • email       (邮件验证)

【第四步】分析验证流程
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
完整的设备验证流程可能包括：

情景A：邮件验证（最常见）
  1. 登录响应包含 "需要验证"、"验证链接"、"验证令牌" 等信息
  2. 用户点击邮件中的验证链接
  3. 浏览器跳转到 /auth/device-verify?token=xxx
  4. 后端自动验证设备并登录用户

情景B：自动验证
  1. 登录后自动调用验证 API (例如 POST /auth/device-verify-confirm)
  2. 返回验证结果
  3. 如果成功，用户已登录

情景C：两步验证码
  1. 登录后显示输入框要求输入验证码
  2. 验证码通过邮件/短信/认证器发送
  3. 输入后提交验证
  4. 登录完成

【第五步】获取您的完整网络流量（最重要）
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

请使用以下方法之一导出网络请求：

▶ 方法1：导出为 HAR 文件
  1. 在 Network 标签中右键点击任何请求
  2. 选择 "Save as HAR with content"
  3. 保存到文件
  4. 分享该文件

▶ 方法2：逐个复制请求信息
  1. 对每个涉及验证的请求：
  2. 右键 → "Copy" → "Copy as cURL"
  3. 贴到文本中

▶ 方法3：直接描述
  对每个相关的 API 请求，提供：
  • URL (例如: https://www.cordc.net/auth/device-verify)
  • 请求方法 (GET/POST)
  • 请求头 (Headers)
  • 请求体 (Payload/Body)
  • 响应状态码
  • 响应体 (Response)

【示例】
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

POST /auth/login
Content-Type: application/x-www-form-urlencoded

email=your@email.com&passwd=...&altcha=...

Response:
{
  "ret": 3,
  "msg": "检测到陌生设备，需要进行二步验证",
  "verify_token": "abc123",
  "verify_url": "https://www.cordc.net/auth/device-verify?token=abc123"
}

════════════════════════════════════════════════════════════════

📋 收集完毕后，请将所有信息分享给我，我会：
1. 分析设备验证的完整流程
2. 实现自动化验证
3. 修复登录流程

感谢您的帮助！
""")
